# README carry-forward — R4
Record Phase 4.6 R4 durable handoff, atomic/checksum readback, no DB migration, no channel write during install/confirm, EN/BG/RU completeness gate, and explicit Publish Review handoff. Preserve REL-SELFTEST-001/002 and active-runtime E2E rule.


R4 FIX2 / REL-SELFTEST-002:
- additive phases must preserve earlier frozen contract literals/semantics where
  later tests and audit logic depend on them;
- do not "fix" a regression by weakening the old test;
- represent the new behavior as an explicit additive persistence upgrade.


R4 FIX3 / REL-SELFTEST-002 extension:
- E2E simulations must propagate source identity through the entire path,
  including persistence; do not stop simulation at successful discovery/fetch.
- SEO metadata and customer-facing short copy are separate outputs. Exact or
  near-duplicate Meta/Short content is a release-blocking quality defect.


R4 FIX4 / REL-SELFTEST-002:
- multilingual quality rules must execute against the real generator for every
  registered language, not only static source text;
- Meta and Short purpose separation is global and future-language-safe.


R4 FIX5 / REL-SELFTEST-002 extension:
- simulate the success branch through final template-context construction;
- validate helper-call keyword signatures with AST/static contract checks;
- safe exception boundaries must include post-operation UI rendering.


R4 FIX5A / REL-SELFTEST-002 extension:
- installer prestate must reflect the operator's actually accepted checkpoint,
  not merely the latest package generated by the assistant;
- if an intermediate package was never accepted, consolidate its changes into
  the next package rather than requiring an unproven state.


R4 FIX5B / REL-SELFTEST-002 extension:
- installer working-tree gates must distinguish governed runtime state from
  source-code drift;
- runtime exceptions must use exact narrow patterns, never broad `var/` ignores;
- installer must preserve runtime state rather than deleting it to obtain green.


R4 FIX5C / REL-SELFTEST-002:
- distinguish weakening from stricter contract evolution;
- migrate obsolete weaker assertions to the new stronger semantics;
- prove monotonicity and preserve executable regression coverage.


R4 FIX5D / REL-SELFTEST-002 extension:
- installer self-test must include a symbol/namespace sanity scan, not only
  syntax compilation;
- compile success is insufficient because undefined runtime names such as ROOT
  are valid syntax but fail only when the branch executes.


R4 FIX5E / REL-SELFTEST-002:
- package-local files must be tested in package-local context;
- repo pytest must not assume release-driver/installer files are copied into the repo;
- self-tests must simulate the same working directory used by the real pytest subprocess.


R4 FIX5F / REL-SELFTEST-002:
- static AST checks themselves must have their runtime dependencies exercised;
- pre-delivery installer validation must execute the helper/import path, not only
  parse the installer source;
- undefined imported-module names are runtime failures even when syntax compiles.


R4 FIX5G / REL-SELFTEST-002:
- pytest working directory is an external runtime variable;
- tests must anchor repository paths from `__file__` or an explicit fixture,
  never assume CWD;
- pre-delivery simulation must include repository-root pytest invocation.


R4 FIX5H / REL-SELFTEST-002:
- simulations must model post-install repository topology, not archive/payload topology;
- distinguish package staging layout from installed destination layout;
- derive test paths from the real `<repo>/tests` and `<repo>/admin-platform/app` contract.


R4 FIX5J / REL-SELFTEST-002:
- full maintained regression must be checked for old package-local path assumptions;
- package-local installer behavior is validated in the installer process;
- repo pytest must remain self-contained;
- BAT must faithfully propagate Python installer exit codes.


R4 FIX5K / REL-SELFTEST-002:
- pre-install and post-install Git gates must use the same governed-state model;
- runtime sidecars are not source drift and must be filtered by the same exact,
  narrow classifier at every working-tree equality gate;
- deliberately migrated legacy regression files are governed source changes and
  must be explicitly listed in the allowed post-install source set.
