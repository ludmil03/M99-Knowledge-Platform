# README v14 carry-forward — Phase 4.6 R3

Record REL-SELFTEST-002 proactive adjacent-defect search and maximal practical simulation before operator delivery. Record R2 Windows acceptance: 587 maintained tests PASS, 6 warnings, all 10 gates PASS on 2026-09-07. RIVER colors/sizes/availability evidence chain accepted; content quality remained insufficient. R3 adds governed Manufacturer Intelligence + Canonical Content. Preserve dynamic BG/EN/RU/RO/GR+future registry, current channel defaults, Amazon/Zalando/eBay + Apple benchmark framework, official manufacturer factual priority, operator-confirmed exact-reference manufacturer evidence, and no invented claims.


R3 FIX2 runtime evidence:
- ImportJobItem.detection is not mapped in current runtime.
- R2 durable DRAFT evidence persistence must not be claimed yet.
- R3 Manufacturer + Content is allowed as governed READ/VERIFY/PREVIEW.
- Production publish must require durable approved content after a separate persistence contract.


R3 FIX4:
- real browser acceptance showed file-level router registration != active FastAPI route registration;
- future web-route releases must import active app.main:app and verify exact route path/method;
- Canonical Preview -> Content Intelligence must propagate source_uuid/product_url from actual request query params;
- this becomes part of REL-SELFTEST-002.


R3 FIX5 / REL-SELFTEST-002:
- active-runtime probes must satisfy required env prerequisites process-locally;
- never weaken M99_SESSION_SECRET enforcement for testing;
- temporary secrets must be random, child-process-only and never logged/persisted;
- verify route METHOD + PATH, not path alone.


R3 FIX6 / REL-SELFTEST-002 carry-forward:
- simulate the full active runtime context: cwd + PYTHONPATH + required env guards;
- relative resources must be checked before app import;
- filesystem router registration is not sufficient;
- exact route method+path must be present in the imported active app graph.


R3 FIX7 / REL-SELFTEST-002:
- define one canonical runtime root and reuse it everywhere;
- self-test must detect undefined runtime path symbols before operator delivery;
- validate the exact generated Jinja navigation fragments, not an assumed quote style;
- labels/backup/recovery messages must match the actual revision.


R3 FIX8 / REL-SELFTEST-002:
- do not ask operators to re-enter Manufacturer URLs already present in approved source registry;
- support Manufacturer UNKNOWN as a first-class non-blocking state;
- distinguish supplier reference from verified Manufacturer product code;
- exact product-page URLs must be tested directly before broad site crawling;
- operator GET/POST workflows must simulate exception paths and must not expose raw 500 pages.


R3 FIX9 / REL-SELFTEST-002:
- every new release driver must derive prestate from the full previously accepted
  cumulative state, not from an earlier phase-only snapshot;
- expected accepted files must never be reported as unexpected drift;
- prestate simulation must include all prior successful additive runtime files.


R3 FIX10 / REL-SELFTEST-002:
- mock/simulation contracts must preserve real helper return types;
- collection composition boundaries must be tested with tuple, list and empty
  sequence variants;
- successful UI resolution is not enough: simulate the button's service path
  through queue construction before operator delivery.


R3 FIX11 / REL-SELFTEST-002:
Simulations must continue beyond discovery into provisional content generation; normalize tuple/list evidence at every merge boundary.
